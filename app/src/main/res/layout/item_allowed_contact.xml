<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <ImageView
        android:id="@+id/ivContactIcon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:contentDescription="@string/menu_item_icon_desc"
        android:src="@android:drawable/sym_action_call" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvAllowedContactName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/main_text"
            android:textStyle="bold"
            android:textSize="16sp"
            android:text="Контакт" />

        <TextView
            android:id="@+id/tvAllowedContactNumber"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/muted_text"
            android:textSize="14sp"
            android:text="+70000000000" />
    </LinearLayout>

    <Button
        android:id="@+id/btnAllowedAddToMain"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:text="@string/btn_add_to_main"
        android:textSize="12sp"
        android:textColor="@color/main_text"
        android:backgroundTint="@color/tile_app"
        android:layout_marginEnd="8dp" />

    <Button
        android:id="@+id/btnAllowedDelete"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:minWidth="90dp"
        android:text="@string/btn_delete"
        android:textSize="12sp"
        android:textColor="@color/main_text"
        android:backgroundTint="@color/tile_sos" />

</LinearLayout>
